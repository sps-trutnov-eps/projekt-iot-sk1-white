<div id="deleteSensorModal" class="fixed inset-0 z-50 hidden" aria-labelledby="modal-title" role="dialog" aria-modal="true">
    <div class="fixed inset-0 bg-red-900/20 backdrop-blur-sm transition-opacity"></div>

    <div class="fixed inset-0 z-10 w-screen overflow-y-auto">
        <div class="flex min-h-full items-center justify-center p-4 text-center sm:p-0">
            <div class="relative transform overflow-hidden rounded-xl bg-white text-left shadow-2xl transition-all sm:my-8 sm:w-full sm:max-w-md border border-red-100">
                
                <div class="bg-red-50 px-4 py-3 sm:px-6 border-b border-red-100 flex justify-between items-center">
                    <h3 class="text-base font-bold leading-6 text-red-700">
                        <i class="fas fa-exclamation-triangle mr-2"></i> Smazat senzor
                    </h3>
                    <button type="button" id="deleteSensorClose" class="text-red-400 hover:text-red-600 transition-colors">
                        <i class="fas fa-times"></i>
                    </button>
                </div>

                <div class="px-4 py-5 sm:p-6">
                    <div id="deleteSensorError" class="hidden bg-red-100 text-red-700 text-xs p-3 rounded-md border border-red-200 mb-4">
                        <p class="font-bold"></p>
                    </div>

                    <div class="flex items-center gap-4">
                        <div class="w-12 h-12 rounded-full bg-red-100 flex items-center justify-center shrink-0">
                            <i class="fas fa-trash-alt text-red-600 text-xl"></i>
                        </div>
                        <div>
                            <p class="text-sm font-bold text-gray-900">Opravdu chcete smazat tento senzor?</p>
                            <p class="text-xs text-gray-500 mt-1">Tato akce je nevratná a dojde k odstranění veškeré historie měření pro tento senzor.</p>
                        </div>
                    </div>
                    
                    <input type="hidden" id="deleteSensorIdInput">
                </div>

                <div class="bg-gray-50 px-4 py-3 sm:flex sm:flex-row-reverse sm:px-6 gap-2">
                    <button type="button" id="deleteSensorSubmit" class="inline-flex w-full justify-center rounded-md bg-red-600 px-3 py-2 text-sm font-semibold text-white shadow-sm hover:bg-red-500 sm:ml-3 sm:w-auto transition-colors">
                        Smazat senzor
                    </button>
                    <button type="button" id="deleteSensorCancel" class="mt-3 inline-flex w-full justify-center rounded-md bg-white px-3 py-2 text-sm font-semibold text-gray-900 shadow-sm ring-1 ring-inset ring-gray-300 hover:bg-gray-50 sm:mt-0 sm:w-auto transition-colors">
                        Zrušit
                    </button>
                </div>
            </div>
        </div>
    </div>
</div>