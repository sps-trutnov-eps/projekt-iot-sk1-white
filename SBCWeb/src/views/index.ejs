<!DOCTYPE html>
<html lang="cs">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= title %></title>
  <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gradient-to-br from-slate-50 to-slate-100 min-h-screen flex items-center justify-center p-4">
  <div class="w-full max-w-md">
    <!-- Logo & Header -->
    <div class="text-center mb-8">
      <div class="inline-block mb-4">
        <div class="w-16 h-16 bg-gradient-to-br from-blue-500 to-cyan-500 rounded-2xl shadow-lg shadow-blue-500/20"></div>
      </div>
      <h1 class="text-3xl font-bold text-slate-800 mb-2"><%= projectName %></h1>
      <p class="text-slate-500">IoT Control Panel</p>
    </div>

    <!-- Login Card -->
    <div class="bg-white rounded-2xl shadow-xl border border-slate-200 p-8">
      <h2 class="text-2xl font-semibold text-slate-800 mb-6">Přihlášení</h2>
      
      <form id="loginForm" class="space-y-5">
        <!-- Username Field -->
        <div>
          <label for="username" class="block text-sm font-medium text-slate-700 mb-2">
            Uživatelské jméno
          </label>
          <input 
            type="text" 
            id="username" 
            name="username"
            class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition"
            placeholder="Zadejte uživatelské jméno"
            required
          >
        </div>

        <!-- Password Field -->
        <div>
          <label for="password" class="block text-sm font-medium text-slate-700 mb-2">
            Heslo
          </label>
          <input 
            type="password" 
            id="password" 
            name="password"
            class="w-full px-4 py-3 bg-slate-50 border border-slate-200 rounded-lg focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-transparent transition"
            placeholder="Zadejte heslo"
            required
          >
        </div>

        <!-- Remember Me -->
        <div class="flex items-center justify-between">
          <label class="flex items-center">
            <input 
              type="checkbox" 
              class="w-4 h-4 text-blue-500 bg-slate-50 border-slate-300 rounded focus:ring-2 focus:ring-blue-500"
            >
            <span class="ml-2 text-sm text-slate-600">Zapamatovat si mě</span>
          </label>
          <a href="#" class="text-sm text-blue-500 hover:text-blue-600 transition">
            Zapomenuté heslo?
          </a>
        </div>

        <!-- Login Button -->
        <button 
          type="submit"
          class="w-full py-3 bg-gradient-to-r from-blue-500 to-cyan-500 text-white font-medium rounded-lg shadow-lg shadow-blue-500/20 hover:shadow-blue-500/40 hover:from-blue-600 hover:to-cyan-600 transition transform hover:-translate-y-0.5"
        >
          Přihlásit se
        </button>
      </form>
    </div>

    <!-- Footer -->
    <div class="text-center mt-6 text-sm text-slate-500">
      <p>&copy; 2026 Projekt IoT SK1 White</p>
    </div>
  </div>

  <script src="/js/app.js"></script>
</body>
</html>
